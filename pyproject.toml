[project]
name = "dietrich"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100
[tool.pytest.ini_options]
addopts = "-q"
# forced-packaging-2:bootstrap
# forced-recovery-3:exploration
# forced-workbench-4:maintenance
# forced-recovery-5:core-build-out
# forced-inspection-6:expansion
# forced-inspection-7:maintenance
